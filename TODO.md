# TODO

-[] Make credentials shared in a volume so that they can be reused across sandboxes
-[] CLI command to clean up docker sandboxes
-[] CLI command to get insight into running sandboxes etc
-[] Share sessions and conversations via separate volume
